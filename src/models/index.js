import Cliente from "./ClienteModel.js";
import Emprestimo from "./EmprestimoModel.js";

(async () => {

    //await Cliente.sync({force:true}); //vai recriar / salvar no banco toda vez que chamar. (await = esperar essa linha terminar antes de executar o resto)
    //await Emprestimo.sync({force:true});


})();